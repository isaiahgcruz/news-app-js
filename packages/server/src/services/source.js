const getSources = async () => {
  // some logic here
  return [];
};

const getArticles = async () => {
  // console.log(sourceId);
  return [];
};

module.exports = {
  getSources,
  getArticles,
};
